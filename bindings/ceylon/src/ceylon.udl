namespace ceylon {
  string version();

  [Async]
  void run_workspace(sequence<AgentCore> agents);
};

interface AgentCore{
    constructor(string id,string name,string workspace_id,boolean is_leader);

    string id();
    string name();
    string workspace_id();
    boolean is_leader();
};